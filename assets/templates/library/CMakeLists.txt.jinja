#========================================================================== */
#                                  LIBRARY                                  */
#========================================================================== */
message("-- Add library: {{name}}")
add_library({{name}})
target_sources(
    {{name}}
    PRIVATE
        "${CMAKE_CURRENT_SOURCE_DIR}/src/{{name}}.cpp"
    
    PUBLIC
    FILE_SET header_{{name}}
    TYPE HEADERS
    BASE_DIRS
      "${CMAKE_CURRENT_SOURCE_DIR}/include"
    FILES
      "include/{{name}}.hpp"
)